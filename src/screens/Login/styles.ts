import styled from 'styled-components/native';

export default {
  WrapperInput: styled.View`
    width: 100%;
    padding-top: ${({theme}) => theme.spacing.LG}px;
  `,

  WrapperHeader: styled.View`
    padding-top: 60px;
  `,
};
